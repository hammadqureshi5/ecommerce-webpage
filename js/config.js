/**
 * Frontend config — change API_BASE when you deploy the backend.
 * The shop UI does not depend on the backend being up.
 */
const VTON_CONFIG = {
  API_BASE: "http://127.0.0.1:8081",
  // Set true when you are ready to call POST /api/generate from the widget.
  API_ENABLED: true,
};
